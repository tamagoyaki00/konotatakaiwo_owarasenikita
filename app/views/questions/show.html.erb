<div class="container mx-auto p-4 sm:p-6 lg:p-8">
  <div class="flex flex-col lg:flex-row lg:space-x-8">

    <div class="flex-grow lg:w-2/3 mb-8 lg:mb-0">
      <div class="bg-white shadow-lg rounded-lg p-6 mb-6 border border-gray-200">
        <h1 class="text-3xl font-extrabold mb-4"><%= @question.title %></h1>

        <div class="mb-6">
          <p class="text-lg mb-2"><%= t('.choose')%></p>
          <div class="space-y-4">
            <% @question.options.each do |option| %>
              <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 flex items-center justify-between hover:bg-blue-100 transition-colors duration-200 cursor-pointer">
                <span class="text-xl font-semibold text-dark-blue"><%= option.content %></span>
                <%# 今後、投票ボタンや投票数をここに追加 %>
              </div>
            <% end %>
          </div>
        </div>


        <div class="flex items-center space-x-3 mt-8 pt-4 border-t border-gray-200">
          <img src="<%= @question.user.google_image_url %>" alt="<%= @question.user.name %>" class="w-10 h-10 rounded-full">
          <span class="font-md"><%= t('.creater')%>: <%= @question.user.name %></span>
        </div>
      </div>
    </div>

  </div>
</div>