<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "この戦いを終わらせに来た！！" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <script src="https://kit.fontawesome.com/9eebda5a5d.js" crossorigin="anonymous"></script>
    <%= display_meta_tags(default_meta_tags) %>
  </head>

  <body class="<%= body_classes %> flex flex-col bg-amber-50 text-black font-noto-sans">
    <div class="flex-shrink-0">
      <%= render "shared/header"%>
    </div>

    <main class="flex-1 overflow-y-auto relative">
      <div id="flash" class="fixed top-16 right-0 p-4 z-50 pointer-events-none">
        <%= render "shared/flash" %>
      </div>
      <%= yield %>
    </main>
    <footer class="flex-shrink-0">
      <%= render "shared/footer"%>
    </footer>
  </body>
</html>
